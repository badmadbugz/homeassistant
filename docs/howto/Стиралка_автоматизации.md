# Автоматизации: стиральная машина

Описание автоматизаций, связанных со стиральной машиной. Устройства: умная розетка (мониторинг потребления), датчик открытия дверцы, **Запасная кнопка** (Zigbee) для переключения отложенного старта.

---

## Устройства

| Устройство | Назначение |
|------------|------------|
| Кухня, розетка, стиралка | `switch.kukhnia_rozetka_stiralka`, потребление `sensor.kukhnia_rozetka_stiralka_power` |
| Стиралка, новый датчик открытия | `binary_sensor.stiralka_novyi_datchik_otkrytiia_contact` |
| Запасная кнопка | Zigbee2MQTT, `sensor.zapasnaia_knopka_action` — переключение отложенного старта |

---

## Хелперы

| Entity | Описание |
|--------|----------|
| `input_boolean.stiralka_otlozhennyi_starrt` | Режим отложенного старта (вкл/выкл) |
| `input_boolean.opoveshchenie_o_stirke` | Буфер для напоминалки (стирка завершена, но дверь не открыта) |

---

## Автоматизации

### 1. Стиралка, автостарт

**ID:** `1740732304448`  
**Назначение:** При потреблении розетки > 10 Вт, если включён отложенный старт и время 07:30–23:00 — отключает розетку (машина «ждёт» питания до 11:01 или 23:01).

**Триггер:** Потребление `sensor.kukhnia_rozetka_stiralka_power` выше 10 Вт  
**Условия:** время 07:30–23:00, `input_boolean.stiralka_otlozhennyi_starrt` = on  
**Действие:** выключить розетку стиралки

---

### 2. Стиралка, включение/отключение отложенного старта

**ID:** `1740732736136`  
**Назначение:** По нажатию **Запасной кнопки** (single) переключает режим отложенного старта.

**Триггер:** Запасная кнопка, одинарное нажатие (MQTT action single)  
**Условие:** розетка стиралки включена  
**Действие:** `input_boolean.toggle` для `stiralka_otlozhennyi_starrt`

---

### 3. Стирка, запуск в 11:01, 23:01

**ID:** `1740969070514`  
**Назначение:** Включить розетку стиралки в 11:01 и 23:01 — фактический старт отложенной стирки.

**Триггер:** Время 11:01 и 23:01  
**Условие:** `input_boolean.stiralka_otlozhennyi_starrt` = on  
**Действие:** включить розетку стиралки

---

### 4. Индикатор отложенной стирки

**ID:** `1741770762698`  
**Назначение:** Включает/выключает индикаторную сцену (свет) и отправляет уведомление при изменении режима отложенного старта.

**Триггер:** Изменение `input_boolean.stiralka_otlozhennyi_starrt`  
**Условие:** розетка стиралки включена  
**Действие:**  
- если on → `scene.otlozhennaia_stirka_vkl` + notify «Отложенная стирка включена»  
- если off → `scene.otlozhennaia_stirka_vykl_duplicate` + notify «Отложенная стирка отключена»

---

### 5. Напоминалка о стирке v3

**ID:** `1758881600512`  
**Назначение:** Если стирка закончена (потребление падает), а дверцу не открыли — напомнить. Если открыли — сбросить и вернуть индикатор.

**Триггер:** `sensor.kukhnia_rozetka_stiralka_power` > 10 Вт в течение 2 минут  
**Действия:**  
1. Включить `input_boolean.opoveshchenie_o_stirke`  
2. Ожидать: либо потребление ≈ 0 (2 мин 10 сек), либо открытие дверцы  
3. Если открыли дверцу — сбросить флаг  
4. Если не открыли: мигнуть светом, отправить «🧺 Стирка закончена», дождаться открытия, вернуть сцену отложенной стирки (вкл/выкл), сбросить флаг

---

### 6. Стиралка, свет v2

**ID:** `1769637420861`  
**Назначение:** Включать/выключать свет на кухне вместе с розеткой стиралки.

**Триггер:** Изменение состояния розетки стиралки  
**Действие:** если розетка on → включить `light.pir_rgbcw_light_kukhnia`, если off → выключить

---

## Сцены

- `scene.otlozhennaia_stirka_vkl` — индикатор «отложенная стирка вкл»
- `scene.otlozhennaia_stirka_vykl_duplicate` — индикатор «отложенная стирка выкл»

---

## Примечание

**Запасная кнопка** используется для переключения отложенного старта (автоматизация 2). Для кормушки (добавить 2150 г) можно использовать **кнопку 0xa4c1386827b1f4da** (Loginovo Smart button, кухня), если она свободна.
